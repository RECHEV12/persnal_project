<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.study.product.dao.IProductDAO">
    <select id="getProdList" resultType="com.study.product.vo.ProductVO" parameterType="com.study.product.vo.ProductSearchVO">
        SELECT
            prod_no,
            prod_title,
            prod_content,
            prod_total_sell,
            prod_price,
            prod_image,
            prod_cate_code,
            TO_CHAR(prod_reg_date,'YYYY-MM-DD') AS prod_reg_date,
            prod_del_yn,
            prod_user_id
        FROM
            products
        WHERE
        prod_del_yn = 'N'
        <if test="@org.apache.commons.lang3.StringUtils@isNotBlank(search.searchWord)">
            <if test='search.searchType  == "name"'>AND prod_title Like '%' || #{search.searchWord} || '%'</if>
            <if test='search.searchType  == "user"'>AND prod_user_id Like '%' || #{search.searchWord} || '%'</if>
        </if>
    </select>
</mapper>